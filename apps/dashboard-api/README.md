# dashboard-api

Node.js + TypeScript API service for dashboard data.

## Test layout

- `test/*.ts` stores source test files only.
- `dist/test/*.js` is generated by `npm run build`.
- Do not commit JavaScript test files under `test/`.

## Common commands

```bash
npm run build
npm run test
```
