export function SiteFooter() {
  return (
    <footer className="border-t border-slate-900/70 bg-base/80">
      <div className="mx-auto flex w-full max-w-6xl flex-col items-start gap-2 px-6 py-6 text-xs text-slate-500 md:flex-row md:items-center md:justify-between">
        <span>? 2026 Developer Studio</span>
        <span>Blog ? Labs ? Tracker</span>
      </div>
    </footer>
  );
}
